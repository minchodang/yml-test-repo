name: Sync Upstream Src and Create PR

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *" # 매일 밤 12시 정각에 실행

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout Fork
        uses: actions/checkout@v3
        with:
          repository: ${{ github.repository }}
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "minchodang"
          git config user.email "minsu910725@gmail.com"

      - name: Add Upstream Remote
        run: |
          git remote add upstream https://github.com/react-hook-form/documentation
          git fetch upstream

      - name: Get src/ changes
        run: |
          # upstream/master와 origin/master의 src/ 디렉토리 차이 추출
          git diff upstream/master origin/master -- src/ > src_changes.diff
          if [ -s src_changes.diff ]; then
            echo "changes=true" >> $GITHUB_ENV
          else
            echo "changes=false" >> $GITHUB_ENV
          fi
          echo "Changes detected in src/:"
          cat src_changes.diff
        shell: bash

      - name: Check Existing PRs
        id: existing-prs
        run: |
          # 기존 PR 가져오기
          RESPONSE=$(curl -s \
              -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
              -H "Accept: application/vnd.github.v3+json" \
              https://api.github.com/repos/${{ github.repository }}/pulls)

          # "sync-upstream-src-"으로 시작하는 PR 필터링
          EXISTING_PR=$(echo "$RESPONSE" | jq -r '.[] | select(.head.ref | startswith("sync-upstream-src-")) | .head.ref' | tr '\n' ' ')

          if [[ -n "$EXISTING_PR" ]]; then
              echo "existing_pr=true" >> $GITHUB_ENV
              echo "existing_pr_ref=$EXISTING_PR" >> $GITHUB_ENV
          else
              echo "existing_pr=false" >> $GITHUB_ENV
          fi
          echo "Existing PRs: $EXISTING_PR"
        shell: bash

      - name: Compare Changes with Each Existing PR
        id: compare-changes
        if: env.existing_pr == 'true' && env.changes == 'true'
        run: |
          ALL_EXISTING_INCLUDED=true
          for pr_ref in ${{ env.existing_pr_ref }}
          do
            echo "Fetching branch: $pr_ref"
            git fetch origin "$pr_ref:$pr_ref-branch"

            echo "Comparing with upstream/master src/ directory"
            if ! git diff --quiet upstream/master "$pr_ref-branch" -- src/; then
              ALL_EXISTING_INCLUDED=false
              break
            fi
          done

          if [ "$ALL_EXISTING_INCLUDED" = true ]; then
            echo "diff=false" >> $GITHUB_ENV
          else
            echo "diff=true" >> $GITHUB_ENV

            # 기존 PR 닫기
            for pr_ref in ${{ env.existing_pr_ref }}
            do
              PR_NUMBER=$(curl -s \
                -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                -H "Accept: application/vnd.github.v3+json" \
                https://api.github.com/repos/${{ github.repository }}/pulls \
                | jq -r '.[] | select(.head.ref=="'$pr_ref'") | .number')

              echo "Closing PR #$PR_NUMBER"
              curl -X PATCH \
                -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                -H "Accept: application/vnd.github.v3+json" \
                https://api.github.com/repos/${{ github.repository }}/pulls/$PR_NUMBER \
                -d '{"state":"closed"}'
            done
          fi
        shell: bash

      - name: Create Branch and Apply src/ Changes
        if: env.diff == 'true' || env.existing_pr == 'false'
        run: |
          set -x # 디버깅 로그 활성화
          BRANCH_NAME=sync-upstream-src-$(date +%Y%m%d%H%M%S)
          git checkout -b $BRANCH_NAME origin/master

          # upstream/master의 src/ 디렉토리 변경 사항 적용
          git checkout upstream/master -- src/

          # .github/workflows/ci.yml 파일 존재 여부 확인 및 제거
          if [ -f .github/workflows/ci.yml ]; then
            git rm --cached .github/workflows/ci.yml || true
            rm .github/workflows/ci.yml
            echo "Removed .github/workflows/ci.yml to exclude workflows changes."
          else
            echo ".github/workflows/ci.yml does not exist. Skipping removal."
          fi

          # 모든 변경 사항 스테이지 해제
          git reset

          # 현재 스테이지된 파일 확인
          echo "After git reset:"
          git status

          # src/ 디렉토리만 스테이지
          git add src/

          # 스테이지된 파일 목록 출력
          echo "Staged files after adding src/:"
          git diff --cached --name-only

          # 스테이지된 파일이 src/ 디렉토리 내 파일만 포함하는지 확인
          STAGED_FILES=$(git diff --cached --name-only)
          echo "Staged files:"
          echo "$STAGED_FILES"

          if [ -n "$(echo "$STAGED_FILES" | grep -v '^src/')" ]; then
            echo "Error: Some files outside src/ are staged."
            exit 1
          fi

          # 커밋
          if ! git diff --quiet --cached; then
            git commit -m "Sync src/ with upstream"
          else
            echo "No changes to commit in src/."
            exit 0
          fi

          # 커밋에 워크플로우 파일 변경 사항이 포함되지 않았는지 확인
          if git show --name-only --pretty="" | grep '^.github/workflows/ci.yml'; then
            echo "Error: Commit includes .github/workflows/ci.yml"
            exit 1
          fi

          # 최종 상태 확인
          echo "Final git status before push:"
          git status

          # 브랜치 푸시
          git push origin $BRANCH_NAME --force-with-lease
          echo "BRANCH_NAME=$BRANCH_NAME" >> $GITHUB_ENV
        shell: bash

      - name: Create Pull Request
        if: env.diff == 'true' || env.existing_pr == 'false'
        run: |
          curl -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/${{ github.repository }}/pulls \
            -d '{
              "title": "Sync src/ with upstream",
              "body": "This PR syncs the src/ directory with upstream repository",
              "head": "'"${{ env.BRANCH_NAME }}"'",
              "base": "master"
            }'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
